var util = require("util");
require("angular");

var orderNewCtrl = function(app){
    app.controller("orderNewCtrl", ['$rootScope', '$scope', function(rootScope, scope){
        
    }]);
};

module.exports = orderNewCtrl;